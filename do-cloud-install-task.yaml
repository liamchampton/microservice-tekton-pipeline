apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: do-cloud-install-task
spec:
  params:
      - name: file2
        type: string
      - name: uuid
        type: string
  steps:
    - name: login-and-deploy-function
      image: digitalocean/doctl:1.55.0
      script: |
        #!/usr/bin/env sh 
       
        # yum install zip -y
        # yum install unzip -y
        # yum install wget -y

        # mkdir -p ./application
        # wget $(params.file2) -O goapp.zip
        # unzip ./goapp.zip -d ./application
        # rm goapp.zip
        # echo $(params.uuid)
        # cd application/microservice-go-demo-main
        # ls -la
        pwd
        doctl -h
        